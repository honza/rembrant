#!/usr/bin/env node

var path = require('path');
var fs   = require('fs');
var root = path.join(path.dirname(fs.realpathSync(__filename)), '../');
var lib  = path.join(root, 'lib');

require(lib + '/rembrant.js').runFromCli()
